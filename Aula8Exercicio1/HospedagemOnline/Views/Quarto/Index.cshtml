@model IEnumerable<HospedagemOnline.Models.Quarto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Novo Quarto", "Inserir")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Alterar", "Alterar", new { id=item.IdQuarto }) |
            @Html.ActionLink("Excluir", "Excluir", new { id=item.IdQuarto })
        </td>
    </tr>
}

</table>
